import './ticket.scss';

const Ticket = (props) => {

    const {ticket} = props;

    console.log(ticket);

    return(
        <div>
            Hello World
        </div>
    )
}

export default Ticket;